doctype html
html(lang="en")
    head
        include partial/header

    body.login 
        .contenedor-formulario 
            h1= nombrePagina
            
            if locals.mensajes 
                .alertas 
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                    p= error                            
                    

            p Agrega tu e-mail para reestablecer tu contraseña 
            form.caja-login(method="POST" action="/reestablecer")
                .campo
                    label(for="email") Correo 
                    input(type="email" name="email" placeholder="Tu email" required value= email)
                .campo.enviar
                    input.boton(type="submit" value="Reestablecer clave")
                .campo.acciones
                    a(href="/iniciar-sesion") Ya lo recorde volver a Login           