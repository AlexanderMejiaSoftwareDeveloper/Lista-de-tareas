doctype html
html(lang="en")
    head
        include partial/header
    body.login
        .contenedor-formulario 
            h1 Iniciar Sesion    
           
            if error 
                p.alerta.error= error        
            
            form#formulario.caja.caja-login(method="POST" action="/iniciar-sesion")
                .campo
                    label(for="email") Correo 
                    input(type="email" name="email" placeholder="Tu email" required value= email)

                .campo
                    label(for="password") Contraseña 
                    input(type="password" name="password" placeholder="Tu contraseña" required value= password)
               
                .campo.enviar
                     input.boton(type="submit" value="Iniciar Sesion")

                .campo 
                    a(href="/crear-cuenta") Aun no tienes cuenta? Crear una gratis
                     